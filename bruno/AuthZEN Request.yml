info:
  name: AuthZEN Request
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/access/v1/evaluation
  body:
    type: json
    data: |-
      {
        "subject":{
          "id": "alice",
          "type": "username",
          "properties":{
            "user.role" : "manager"
          }
        },
        "action":{
          "name": "view"
        },
        "resource":{
          "id": "123",
          "type": "record"
        },
        "context": {}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
